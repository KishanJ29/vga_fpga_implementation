# vga_fpga_implementation
###  VGA controlller (640x480) is implemented in VHDL, Displays image from SRAM 
    .-> VGA.vhd => Top Module 
    .-> vga_cntrl.vhd => VGA controller
    .-> sram_cntrl => SRAM Controller
    .-> vga_image => VGA test file displays single color
    
    
 
